# View

Contains the layout structure for the Home page.  
Each file represents a visual section (Hero, About, Division, etc).

These are all presentational components and should receive data via props.
